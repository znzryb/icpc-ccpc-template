# Computational Geometry templates
# 点线/距离/多边形/凸包/极角排序: :Point + :reorderPolygon / :polarAngleSort / :isConvex /
#                                  :makeConvexHull / :minkowski /
#                                  :polygonArea / :polygonPerimeter / :regularPolygonArea / :isInPolygon
# 角度: :ccwAngle (逆时针弧角, 标量 + 向量两版)
# 圆: :Circle (核心: enum + struct Circle) 拆分系列:
#   :circleRelation / :incircleOutcircle / :getCrossPointsCL / :getCrossPointsCC /
#   :circleTangent / :twoCircleArea / :polygonCircleArea / :minCircleCover
# 工具: :gen_convex_polygon / :convexRotatingCalipers (旋转卡壳) / :halfPlaneCut

matches:
- trigger: :Point
  label: Point类，Line类 (核心)
  replace: "\n/*\n * 2026-2-3  增加编译器识别 Real 类型功能\n * 2026-3-19 增加了从斜率式和一般式构造 Line\n * 2026-5-4  几何模板大改（QOJ 784 沉淀）：新增 isqrt / sqrtL、cross 三参数重载、polygonSignedArea，reorder_polygon 整体判向，isConvex 加 strict 参数，新增凸多边形直径旋转卡壳\n * 2026-5-5  Luogu P16223 沉淀：Point 加 id 字段，convex_diameter 加 n <= 2 退化特判，新增 furthest_pair_of_point\n * 2026-5-6  P3187 沉淀：getintersect 加浮点 assert + 显式 (DB) cross，make_convex_hull 加 strict 参数，新增 minimum_rectangle_cover\n * 2026-5-11 几何全面模板化：Point / Line 改 template<class T>，T 自动分流 ll / i128 / DB\n * 2026-5-12 加入了 Circle 类\n * 2026-5-13 加入了 half_plane_cut 和 unique_line_polar_angle_sort，加入了隐式转换和转换方向\n * 2026-5-15 加入了 HullMode\n */\n\n// floor(sqrt(n)), n >= 0；二分 + 防溢出\ni128 isqrt(i128 n) {\n\tif (n <= 0) return 0;\n\ti128 lo = 0, hi = (i128)2e18; // sqrt(4e36) <= 2e18\n\twhile (lo < hi) {\n\t\ti128 mid = lo + (hi - lo + 1) / 2;\n\t\tif (mid <= n / mid) lo = mid; // 等价 mid*mid <= n，避免 i128 平方溢出\n\t\telse hi = mid - 1;\n\t}\n\treturn lo;\n}\n\n// 统一 sqrt 入口：浮点直接 sqrtl；整数（含 i128）走 isqrt + fraction，避免 (long double) i128 在 n > 2^53 时丢整数精度。\ntemplate<typename T>\nlong double sqrtL(T n) {\n\tif constexpr (is_floating_point_v<T>) {\n\t\treturn sqrtl((long double)n);\n\t} else {\n\t\ti128 ni = (i128)n;\n\t\tif (ni <= 0) return 0;\n\t\ti128 i = isqrt(ni);\n\t\tlong double id = (long double)i;\n\t\tlong double rd = (long double)(ni - i * i);\n\t\t// sqrt(i^2 + r) = i * sqrt(1 + r/i^2)\n\t\treturn id * sqrtl(1.0L + rd / (id * id));\n\t}\n}\n\n// x*x 安全平方: 整数升 i128 防溢出 (1000^2 不会溢, 但模板要扛 1e9 量级输入),\n// 浮点原样 T*T 不动 (i128 没浮点意义).\ntemplate<class T>\nauto squared(T x) {\n\tif constexpr (is_floating_point_v<T>) {\n\t\treturn x * x;\n\t} else {\n\t\treturn (i128)x * (i128)x;\n\t}\n}\n\ntemplate<class T>\nint sgn(T x) {\n\tif (-eps < x && x < eps) return 0;\n\treturn x < 0 ? -1 : 1;\n}\n\ntemplate<class T>\nstruct Point {\n\tT x, y;\n\tint id = -1; // 原始下标. -1 表示派生点 (+ - * 等算术结果) 或未赋值, id 无意义\n\n\texplicit Point(T x_ = 0, T y_ = 0) : x(x_), y(y_) {\n\t}\n\n\t// Point<U> -> Point<T> 隐式 (common_type 单调升档方向, U -> T 必须无损):\n\t// common_type_t<U, T> == T 才放行 (例 ll -> DB 通; DB -> ll 拦, 防 truncation)\n\ttemplate<class U, class = enable_if_t<is_same_v<common_type_t<U, T>, T>>>\n\tPoint(const Point<U> &o) : x((T)o.x), y((T)o.y), id(o.id) {\n\t}\n\n\t// 只对左值生效的复合运算符\n\tPoint &operator+=(Point p) & {\n\t\tx += p.x, y += p.y;\n\t\treturn *this;\n\t}\n\n\tPoint &operator-=(Point p) & {\n\t\tx -= p.x, y -= p.y;\n\t\treturn *this;\n\t}\n\n\tPoint &operator*=(T t) & {\n\t\tx *= t, y *= t;\n\t\treturn *this;\n\t}\n\n\tPoint &operator/=(T t) & {\n\t\tx /= t, y /= t;\n\t\treturn *this;\n\t}\n\n\tPoint operator-() const { return Point(-x, -y); }\n\n\tfriend Point operator+(Point a, Point b) { return a += b; }\n\tfriend Point operator-(Point a, Point b) { return a -= b; }\n\n\t// 标量乘除: 返回 Point<common_type_t<T, U>> (C++14), 自动算公共类型\n\t// <ll,ll>=ll / <ll,DB>=DB / <DB,ll>=DB; R==T 走 copy ctor, R!=T 走 converting ctor\n\ttemplate<class U>\n\tfriend auto operator*(Point a, U b) {\n\t\tPoint<common_type_t<T, U>> r = a; r *= b; return r;\n\t}\n\ttemplate<class U>\n\tfriend auto operator*(U a, Point b) {\n\t\tPoint<common_type_t<T, U>> r = b; r *= a; return r;\n\t}\n\ttemplate<class U>\n\tfriend auto operator/(Point a, U b) {\n\t\tPoint<common_type_t<T, U>> r = a; r /= b; return r;\n\t}\n\n\tfriend bool operator<(Point a, Point b) {\n\t\tint sx = sgn(a.x - b.x);\n\t\tif (sx != 0) return a.x < b.x;\n\t\treturn a.y < b.y;\n\t}\n\n\tfriend bool operator>(Point a, Point b) { return b < a; }\n\n\tfriend bool operator==(Point a, Point b) {\n\t\treturn sgn(a.x - b.x) == 0 && sgn(a.y - b.y) == 0;\n\t}\n\n\tfriend bool operator!=(Point a, Point b) { return !(a == b); }\n\n\tfriend istream &operator>>(istream &is, Point &p) { return is >> p.x >> p.y; }\n\tfriend ostream &operator<<(ostream &os, Point p) { return os << \"(\" << p.x << \", \" << p.y << \")\"; }\n\n\tauto norm() const {\n\t\tif constexpr (!is_floating_point_v<T>) {\n\t\t\treturn (__int128_t)x * x + (__int128_t)y * y;\n\t\t} else {\n\t\t\treturn x * x + y * y;\n\t\t}\n\t}\n\n\tDB abs() const { return sqrtL(norm()); }\n\n\tPoint rot90() const {\n\t\treturn Point(-y, x);\n\t}\n\n\tPoint<DB> rot(DB ang) const {\n\t\treturn Point<DB>(cosl(ang) * x - sinl(ang) * y, sinl(ang) * x + cosl(ang) * y);\n\t}\n\n\t// L1 范数 (曼哈顿) = |x| + |y|. 整型分支走 __int128_t 防溢出.\n\tauto normL1() const {\n\t\tauto ax = (x < 0 ? -x : x), ay = (y < 0 ? -y : y);\n\t\tif constexpr (!is_floating_point_v<T>) {\n\t\t\treturn (__int128_t)ax + (__int128_t)ay;\n\t\t} else {\n\t\t\treturn ax + ay;\n\t\t}\n\t}\n\n\t// Linf 范数 (切比雪夫) = max(|x|, |y|). 返回类型与 T 同.\n\tT normLinf() const {\n\t\tauto ax = (x < 0 ? -x : x), ay = (y < 0 ? -y : y);\n\t\treturn ax > ay ? ax : ay;\n\t}\n};\n\ntemplate<class T, class U>\nauto cross(Point<T> a, Point<U> b) {\n\t// 在 T 为整数的时候，使用 __int128_t 整型进行计算\n\t//（改为这个非float，那么在类型为I128时，仍然可以计算）\n\tif constexpr (!is_floating_point_v<T> && !is_floating_point_v<U>) {\n\t\treturn (__int128_t)a.x * b.y - (__int128_t)a.y * b.x;\n\t} else {\n\t\treturn a.x * b.y - a.y * b.x;\n\t}\n}\n\n// 三点重载: 以 o 为原点, 算 (a - o) × (b - o)\n// 用途: 判 3 点 turn (>0 左转, <0 右转, =0 共线) / 2 倍三角形有向面积 / 凸包面积扫描\ntemplate<class T, class U, class V>\nauto cross(Point<T> o, Point<U> a, Point<V> b) {\n\treturn cross(a - o, b - o);\n}\n\ntemplate<class T, class U>\nauto dot(Point<T> a, Point<U> b) {\n\t// 在 T 为整数的时候，使用 __int128_t 整型进行计算\n\tif constexpr (!is_floating_point_v<T> && !is_floating_point_v<U>) {\n\t\treturn (__int128_t)a.x * b.x + (__int128_t)a.y * b.y;\n\t} else {\n\t\treturn a.x * b.x + a.y * b.y;\n\t}\n}\n\n// 三点重载: 以 o 为原点, 算 (a - o) · (b - o)\n// 用途: 判夹角钝/直/锐 (>0 锐, =0 直, <0 钝) / 投影长度的有符号 2 倍 / 点在线段上的位置判定\ntemplate<class T, class U, class V>\nauto dot(Point<T> o, Point<U> a, Point<V> b) {\n\treturn dot(a - o, b - o);\n}\n\n// 采用两点式\ntemplate<class T>\nstruct Line {\n\tPoint<T> p1, p2;\n\n\tLine() = default;\n\n\tLine(Point<T> a, Point<T> b) : p1(a), p2(b) {\n\t}\n\n\t// Line<U> -> Line<T> 隐式 (common_type 单调升档方向, 同 Point converting ctor)\n\ttemplate<class U, class = enable_if_t<is_same_v<common_type_t<U, T>, T>>>\n\tLine(const Line<U> &o) : p1(o.p1), p2(o.p2) {\n\t}\n\n\t// AC https://qoj.ac/submission/2146870\n\tLine(T k, T c) : p1(Point<T>(0, c)), p2(Point<T>(1, k + c)) {\n\t}\n\n\tfriend bool operator<(Line a, Line b) {\n\t\tif (a.p1 != b.p1) return a.p1 < b.p1;\n\t\treturn a.p2 < b.p2;\n\t}\n\n\tfriend bool operator==(Line a, Line b) {\n\t\tif (a.p1 != b.p1) return false;\n\t\tif (a.p2 != b.p2) return false;\n\t\treturn true;\n\t}\n\n\tfriend istream &operator>>(istream &is, Line &e) {\n\t\tT a, b, c, d;\n\t\tis >> a >> b >> c >> d;\n\t\te = Line(Point<T>(a, b), Point<T>(c, d));\n\t\treturn is;\n\t}\n\n\tfriend ostream &operator<<(ostream &os, Line l) {\n\t\treturn os << \"<\" << l.p1 << \", \" << l.p2 << \">\";\n\t}\n};\n\nLine<DB> gen_line_from_general(DB a, DB b, DB c) {\n\t// 方向向量 (b, -a)\n\tPoint<DB> p1, p2;\n\tif (b != 0) {\n\t\tp1 = Point<DB>(0, -c / b);\n\t} else {\n\t\tp1 = Point<DB>(-c / a, 0);\n\t}\n\tp2 = Point<DB>(p1.x + b, p1.y - a);\n\treturn Line<DB>(p1, p2);\n}\n\n// 使用这个东西，Point 必须是 double 等浮点类型\ntemplate<class T, class U>\nPoint<DB> project(Point<T> p, Line<U> l) {\n\tPoint<DB> vec = l.p2 - l.p1;\n\t//                        这个距离反正早除晚除都要除掉\n\tDB r = dot(vec, p - l.p1) / ((DB)vec.x * vec.x + vec.y * vec.y);\n\treturn l.p1 + vec * r;\n}\n\ntemplate<class T, class U>\nPoint<DB> reflect(Point<T> p, Line<U> l) {\n\tPoint<DB> proj = project(p, l);\n\treturn proj + proj - p;\n}\n\nmt19937_64 rng(std::chrono::steady_clock::now().time_since_epoch().count());\n\nconstexpr int ON_SEGMENT = 0;\nconstexpr int COUNTER_CLOCKWISE = 1;\nconstexpr int CLOCKWISE = -1;\nconstexpr int ONLINE_BACK = 2;\nconstexpr int ONLINE_FRONT = -2;\n\ntemplate<class T, class U>\nint CCW(Point<T> p, Line<U> l) {\n\tauto a = l.p2 - l.p1;\n\tauto b = p - l.p1;\n\tif (sgn(cross(a, b)) > 0) {\n\t\treturn COUNTER_CLOCKWISE;\n\t}\n\tif (sgn(cross(a, b)) < 0) {\n\t\treturn CLOCKWISE;\n\t}\n\tif (sgn(dot(a, b)) < 0) {\n\t\treturn ONLINE_BACK;\n\t}\n\tif (a.norm() < b.norm()) return ONLINE_FRONT;\n\treturn ON_SEGMENT;\n}\n\ntemplate<class T, class U>\nbool isOrthogonal(Line<T> a, Line<U> b) {\n\tauto c = a.p2 - a.p1, d = b.p2 - b.p1;\n\treturn !sgn(dot(c, d));\n}\n\ntemplate<class T, class U>\nbool isParallel(Line<T> a, Line<U> b) {\n\tauto c = a.p2 - a.p1, d = b.p2 - b.p1;\n\treturn !sgn(cross(c, d));\n}\n\ntemplate<class T, class U>\nbool isSegIntersect(Line<T> a, Line<U> b) {\n\t// 判断线段相交\n\treturn CCW(b.p1, a) * CCW(b.p2, a) <= 0 && CCW(a.p1, b) * CCW(a.p2, b) <= 0;\n}\n\n// bool isIntersectStrict(Line a,Line b){    // 判断线段相交\n//     return CCW(b.p1,a)*CCW(b.p2,a)<0 && CCW(a.p1,b)*CCW(a.p2,b)<0;\n// }\n// p+tv=q+sw\n// (p-q) x w+tv x w = 0     (利用叉乘乘以自身为0，消掉sw)\n// t=-(p-q)x w/(vxw)\n// 传入的是方向式  拿到的是两直线交点\ntemplate<class T, class U>\nPoint<DB> getintersect(const Line<T> &a, const Line<U> &b) {\n\tPoint<DB> p = a.p1, v = a.p2 - a.p1, q = b.p1, w = b.p2 - b.p1;\n\tPoint<DB> u = p - q;\n\tDB t = (DB)cross(w, u) / cross(v, w);\n\treturn p + v * t;\n}\n\n// 距离平方（不开方）：旋转卡壳里 ans 反复更新，留到最后一次 sqrt\ntemplate<class T, class U>\nauto distancePPNorm(const Point<T> &a, const Point<U> &b) {\n\treturn (a - b).norm();\n}\n\n// L2 欧几里得距离 (sqrt 版, 用 long double). 整型坐标走 isqrt + fraction 不丢精度.\ntemplate<class T, class U>\nDB distancePP(const Point<T> &a, const Point<U> &b) {\n\treturn (a - b).abs();\n}\n\n// L1 曼哈顿距离 = |dx| + |dy|. 返回类型继承 normL1 (整型 -> __int128_t, 浮点 -> T).\ntemplate<class T, class U>\nauto distancePPL1(const Point<T> &a, const Point<U> &b) {\n\treturn (a - b).normL1();\n}\n\n// Linf 切比雪夫距离 = max(|dx|, |dy|). 返回类型 T.\ntemplate<class T, class U>\nT distancePPLinf(const Point<T> &a, const Point<U> &b) {\n\treturn (a - b).normLinf();\n}\n\n// 也可以用 project 来算\ntemplate<class T, class U>\nDB distancePL(Point<T> p, Line<U> l) {\n\tauto val = cross(l.p1, l.p2, p);\n\treturn sgn(val) * val / (l.p2 - l.p1).abs();\n}\n\n// AC https://qoj.ac/submission/2001668\ntemplate<class T, class U>\nDB distancePS(Point<T> p, Line<U> l) {\n\t// 避免 distanceSS 调用的时候发生这个退化（即线段退化为了点）\n\tif (l.p1 == l.p2) {\n\t\treturn (p - l.p1).abs();\n\t}\n\tauto t = l.p2 - l.p1;\n\tif (sgn(dot(t, p - l.p1)) < 0) {\n\t\treturn (p - l.p1).abs();\n\t}\n\tif (sgn(dot(t, p - l.p2)) > 0) {\n\t\treturn (p - l.p2).abs();\n\t}\n\treturn distancePL(p, l);\n}\n\n// AC https://qoj.ac/submission/2001588\ntemplate<class T, class U>\nDB distanceSS(Line<T> a, Line<U> b) {\n\tif (isSegIntersect(a, b)) return 0;\n\treturn min({distancePS(a.p1, b), distancePS(a.p2, b), distancePS(b.p1, a), distancePS(b.p2, a)});\n}\n\n"
- trigger: :reorderPolygon
  label: reorder_polygon (多边形 CCW 整理)
  replace: "// === reorder_polygon (espanso :reorderPolygon, 拆自 :Point) ===\n// 按照逆时针方向整齐排列\ntemplate<class T>\nvoid reorder_polygon(vector<Point<T> > &poly) {\n\tll n = poly.size();\n\t// 用 polygonSignedArea 判方向, 不依赖局部前三点 cross\n\t// (含共线点的 polygon 在前三点共线时, 局部判方向会误判为 CW)\n\tif (sgn(polygonSignedArea(poly)) < 0) {\n\t\treverse(poly.begin(), poly.end());\n\t}\n\t// 找到 y 最小，然后 x 小的点\n\tll pos_mn = 0;\n\tfor (int i = 1; i < n; ++i) {\n\t\tif (poly[i].y < poly[pos_mn].y || (poly[i].y == poly[pos_mn].y && poly[i].x < poly[pos_mn].x)) {\n\t\t\tpos_mn = i;\n\t\t}\n\t}\n\t// Performs a left rotation on a range of elements\n\t// 向左循环移动，把 pos_mn 移动到第一个 [0]\n\trotate(poly.begin(), poly.begin() + pos_mn, poly.end());\n}\n"
- trigger: :polarAngleSort
  label: 极角排序工具包
  replace: "// === polar_angle_sort 套件 (espanso :polarAngleSort, 拆自 :Point)\n//     get_part_of_point + polar_angle_sort(Point) + polar_angle_sort(Line) + unique_line_polar_angle_sort\n// 定义获取区域的辅助函数，辅助\ntemplate<class T>\nint get_part_of_point(const Point<T> &p) {\n\tif (p.y < 0) return 0; // 下半平面 (-pi, 0)\n\tif (p.y == 0 && p.x >= 0) return 1; // 角度为 0 的部分 (含原点)\n\treturn 2; // 上半平面 (0, pi]\n}\n\n// 你需要将所有点按照 atan2(yi,xi)进行排序，也即从 x 轴负半轴（不含）开始进行逆时针排序。\n// AC https://qoj.ac/submission/2001485\ntemplate<class T>\nvoid polar_angle_sort(vector<Point<T> > &poly) {\n\tsort(poly.begin(), poly.end(), [](const Point<T> &a, const Point<T> &b)-> bool {\n\t\tint part1 = get_part_of_point(a), part2 = get_part_of_point(b);\n\t\tif (part1 != part2) {\n\t\t\treturn part1 < part2;\n\t\t}\n\t\treturn sgn(cross(a, b)) == 1;\n\t});\n}\n\n// 重载，用来完成这个对线的极角排序，用于这个半平面交\ntemplate<class T>\nvoid polar_angle_sort(vector<Line<T> > &poly) {\n\tsort(poly.begin(), poly.end(), [](const Line<T> &al, const Line<T> &bl)-> bool {\n\t\tauto a = al.p2 - al.p1, b = bl.p2 - bl.p1;\n\t\tint part1 = get_part_of_point(a), part2 = get_part_of_point(b);\n\t\tif (part1 != part2) {\n\t\t\treturn part1 < part2;\n\t\t}\n\t\tint c = sgn(cross(a, b));\n\t\tif (c != 0) return c == 1;\n\t\t// bl 在 al 的 右侧，因此 al 应该排在前面（返回 true）\n\t\treturn CCW(bl.p1, al) == CLOCKWISE;\n\t});\n}\n\ntemplate<class T>\nvoid unique_line_polar_angle_sort(vector<Line<T> > &poly) {\n\tpolar_angle_sort(poly);\n\tpoly.resize(unique(all(poly), [](const Line<T> &al, const Line<T> &bl) {\n\t\tauto a = al.p2 - al.p1, b = bl.p2 - bl.p1;\n\t\tif (sgn(cross(a, b)) == 0) {\n\t\t\treturn true;\n\t\t}\n\t\treturn false;\n\t}) - poly.begin());\n}\n"
- trigger: :isConvex
  label: 判定凸多边形
  replace: "// === isConvex (espanso :isConvex, 拆自 :Point)\n//     依赖: :Point 基础模板 + :reorderPolygon (内调 reorder_polygon)\n// 不允许自交的多边形，顺序都可以，会把 poly 变为 CCW 顺序（逆时针）\n// AC https://qoj.ac/submission/2002121\n// strict = true:  严格凸 (拒绝三点共线 / 内角 = 180°)\n// strict = false: 弱凸    (允许三点共线 / 内角 = 180°), 题面允许共线时用\ntemplate<class T>\nbool isConvex(vector<Point<T> > poly, bool strict) {\n\tll n = poly.size();\n\tif (n <= 2) return false;\n\treorder_polygon(poly);\n\tauto fail = [&](int s) { return strict ? s <= 0 : s < 0; };\n\t// 在一个标准的凸多边形中，如果我们站在最左下角的点（poly[0]）往外看\n\t// 其他所有顶点 poly[1], poly[2], ..., poly[n-1] 应该按照逆时针方向整齐排列。\n\t// 从而排除星形（自交）多边形和乱序点集。\n\tbool has_turn = false; // 至少一处真左转, 排除全共线 (面积 = 0) 退化\n\tfor (int i = 1; i < n - 1; ++i) {\n\t\tint s = sgn(cross(poly[0], poly[i], poly[i + 1]));\n\t\tif (fail(s)) return false;\n\t\tif (s > 0) has_turn = true;\n\t}\n\t// 弱凸模式下还需 has_turn: 否则 N 点全共线时上面两个循环 cross 全 0,\n\t// 误把\"线段\"判成多边形 (strict 模式 has_turn 在第一个循环必然为 true)\n\tif (!has_turn) return false;\n\t// 判断单个内角是否 ≥ 180。\n\tfor (int i = 0; i < n; ++i) {\n\t\tll i1 = (i + 1) % n, i2 = (i + 2) % n;\n\t\tauto a = poly[i1] - poly[i], b = poly[i2] - poly[i1];\n\t\t// strict 时拒绝内角 = 180 (sgn = 0); 弱凸允许 (只拒绝凹: sgn < 0)\n\t\tif (fail(sgn(cross(a, b)))) {\n\t\t\treturn false;\n\t\t}\n\t}\n\treturn true;\n}\n"
# changelog (espanso 层日志, 不进 replace 代码):
#   2026-05-22  牛客-15086「向左走」沉淀: HULL_FULL 全共线退化输入不再折返吐重复点
#               (有向面积为 0 即退化, 此时沿线单程返回排序后的 A).
#               题目 https://vjudge.net/problem/牛客-15086   AC https://vjudge.net/solution/70029254
#   2026-05-23  牛客-15086「向左走」沉淀: make_convex_hull 排序后加 unique 去重
#               (重复点会污染单调栈/折返判定); sort 随之提到算 n 之前 (unique 改变点数).
#               题目 https://vjudge.net/problem/牛客-15086   AC https://vjudge.net/solution/70036057
#   2026-07-24  牛客 2026 Summer 多校 3-K「Turn by Turn Navigation」沉淀: CCW 内 a/b 由 Point<DB> 改 auto
#               (整数坐标转 double 后, cross/dot ~1e18 超 double 2^53 精确整数范围 -> sgn 判错引入误差;
#                auto 保 a/b 原生推导类型, 整数走 __int128 精确算; 双类型参数签名不动).
#   2026-08-19  牛客 2026 多校 10-A「Natsuhikage」沉淀: Circle 新增 theta_at / check_on_circle
#               (theta_at 是 point_at 的逆, atan2l 求极角并归一化到 [0, 2pi); LOCAL 下断言点确实在圆上);
#               point_at 改走 __builtin_sincos 一次算 sin + cos (省一次三角函数调用), 原 cosl/sinl 版保留为注释备查.
#               注意: 本片段 DB = double 才用 __builtin_sincos; DB = long double 的工程 (如 ICPC 板子) 须改 __builtin_sincosl.
- trigger: :makeConvexHull
  label: 凸包构造 (HullMode + make_convex_hull)
  replace: "// === HullMode + make_convex_hull (espanso :makeConvexHull, 拆自 :Point)\n//     依赖: :Point 基础模板\nenum HullMode { HULL_FULL, HULL_LOWER, HULL_UPPER };\n\n// strict = true:  严格凸包 (不保留三点共线的中间点)\n// strict = false: 弱凸包    (保留三点共线的中间点), 题目要求\"凸包上所有点\"时用\ntemplate<class T>\nvector<Point<T> > make_convex_hull(vector<Point<T> > A, bool strict, HullMode hull_mode) {\n\t// Andrew 扫描. 各 mode 的排序键与返回 (顶点序列都是 CCW 序):\n\t// HULL_FULL : 按 Y 排序 (Y 同按 X), 返回整圈凸包, 从 (y 最小, 再 x 最小) 点起头.\n\t// HULL_LOWER: 按 X 排序 (X 同按 Y), 返回下凸链, 最左 -> 最右 (x 单调不减).\n\t// HULL_UPPER: 按 X 排序 (X 同按 Y), 返回上凸链, 最右 -> 最左 (x 单调不增).\n\tif (hull_mode == HULL_FULL) {\n\t\tsort(A.begin(), A.end(), [](const Point<T> &a, const Point<T> &b) {\n\t\t\treturn a.y != b.y ? a.y < b.y : a.x < b.x;\n\t\t});\n\t} else {\n\t\tsort(A.begin(), A.end());\n\t}\n\tA.resize(unique(all(A)) - A.begin());\n\tsize_t n = A.size();\n\tif (n <= 2) {\n\t\t// 退化: 不足 3 点直接返回排序后的点; UPPER 逆序成 x 不增, FULL/LOWER 保持排序序\n\t\tif (hull_mode == HULL_UPPER) reverse(A.begin(), A.end());\n\t\treturn A;\n\t}\n\tvector<Point<T> > ans(n * 2);\n\t// strict 时 cross == 0 共线也弹; 弱凸只在 cross < 0 凹陷时弹\n\tauto fail = [&](ll s) { return strict ? s >= 0 : s > 0; };\n\tint now = -1;\n\tfor (int i = 0; i < n; i++) {\n\t\t// 维护下凸包\n\t\twhile (now > 0 && fail(sgn(cross(A[i], ans[now], ans[now - 1])))) {\n\t\t\tnow--; // 弹出栈顶，因为它会导致凹陷 (strict 时还会弹掉共线)\n\t\t}\n\t\tans[++now] = A[i];\n\t}\n\tif (hull_mode == HULL_LOWER) {\n\t\tans.resize(now + 1);\n\t\treturn ans;\n\t}\n\tint pre = now;\n\t// 从右向左扫描所有点（注意是从 n-2 开始，因为 n-1 已经在栈顶了）\n\tfor (int i = n - 2; i >= 0; i--) {\n\t\t// 维护上凸包\n\t\twhile (now > pre && fail(sgn(cross(A[i], ans[now], ans[now - 1])))) {\n\t\t\tnow--;\n\t\t}\n\t\tans[++now] = A[i];\n\t}\n\tif (hull_mode == HULL_UPPER) {\n\t\treturn vector<Point<T> >(ans.begin() + pre, ans.begin() + now + 1);\n\t}\n\tans.resize(now);\n\t// HULL_FULL: 因上面按 Y 排序, 这里已天然是「Y 最小点起头 + CCW」.\n\t// 退化处理: 输入点全共线时凸包退化成线段, 弱凸包会折返成重复序列 (如 A B C B);\n\t// 有向面积为 0 即退化, 此时 A 已按 Y 排好序且点互异, 就是沿线单程, 直接返回 (O(n), 不折返).\n\tif (sgn(polygonSignedArea(ans)) == 0 && !strict) {\n\t\treturn A;\n\t}\n\treturn ans;\n}"
- trigger: :minkowski
  label: Minkowski 和
  replace: "// === minkowski 和 (espanso :minkowski, 拆自 :Point)\n//     依赖: :Point 基础模板 + :reorderPolygon (minkowski 内调 reorder_polygon)\n// 两个小凸包，合成一个大凸包，这个大凸包，即闵可夫斯基和\n// 参考了 https://github.com/hh2048/XCPC 的 mincowski\n// minkowski 的减法就是加上关于原点的中心对称图形\n// AC https://qoj.ac/submission/2002545\ntemplate<class T>\nvector<Point<T> > minkowski(vector<Point<T> > &P1, vector<Point<T> > &P2) {\n\treorder_polygon(P1);\n\treorder_polygon(P2);\n\tint n = P1.size(), m = P2.size();\n\tvector<Point<T> > V1(n), V2(m);\n\t// 预处理：计算差分向量（边向量）\n\tfor (int i = 0; i < n; i++) {\n\t\tV1[i] = P1[(i + 1) % n] - P1[i];\n\t}\n\tfor (int i = 0; i < m; i++) {\n\t\tV2[i] = P2[(i + 1) % m] - P2[i];\n\t}\n\tvector<Point<T> > ans = {P1.front() + P2.front()};\n\tint t = 0, i = 0, j = 0;\n\twhile (i < n && j < m) {\n\t\t// cross(V1,V2)> 0，就说明 V1 小于这个 V2(在 CCW 下)\n\t\t// 所以先选择这个小的 V1，反之同理\n\t\tPoint<T> val = sgn(cross(V1[i], V2[j])) > 0 ? V1[i++] : V2[j++];\n\t\tans.push_back(ans.back() + val);\n\t}\n\t// 处理剩余的边\n\twhile (i < n) ans.push_back(ans.back() + V1[i++]);\n\twhile (j < m) ans.push_back(ans.back() + V2[j++]);\n\treturn ans;\n}\n"
- trigger: :polygonArea
  label: polygonSignedArea + polygonArea
  replace: "// === polygonSignedArea + polygonArea (espanso :polygonArea, 拆自 :Point)\n//     依赖: :Point 基础模板 (用到 cross)\n// 多边形 2 倍有向面积 (不除 2 以保持整数精度).\n// 返回类型 auto 自动跟 cross 走 (整数 Real → i128, 浮点 Real → Real).\n// > 0: CCW; < 0: CW; == 0: 退化 (全共线 / 自交).\n// 用途: polygonArea (取 abs / 2 得真面积) / reorder_polygon (取符号判方向).\ntemplate<class T>\nauto polygonSignedArea(const vector<Point<T> > &poly) {\n\tauto res = cross(Point<T>(), Point<T>()); // 零值, 类型由 cross 继承\n\tsize_t n = poly.size();\n\tfor (size_t i = 0; i < n; ++i) {\n\t\tres += cross(poly[i], poly[(i + 1) % n]);\n\t}\n\treturn res;\n}\n\ntemplate<class T>\nDB polygonArea(const vector<Point<T> > &poly) {\n\tauto s = polygonSignedArea(poly);\n\tif (s < 0) s = -s;\n\treturn (DB)s / 2.0;\n}\n"
- trigger: :polygonPerimeter
  label: 多边形周长
  replace: "// === polygon_perimeter (espanso :polygonPerimeter, 拆自 :Point)\n//     依赖: :Point 基础模板 (Point::abs)\n// 求周长\ntemplate<class T>\nDB polygon_perimeter(const vector<Point<T> > &poly) {\n\tDB res = 0;\n\tsize_t n = poly.size();\n\tfor (int i = 0; i < n; ++i) {\n\t\tres += (poly[i] - poly[(i + 1) % n]).abs();\n\t}\n\treturn res;\n}\n"
- trigger: :regularPolygonArea
  label: 正 n 边形面积
  replace: "// === regular_polygon_area (espanso :regularPolygonArea, 拆自 :Point)\n//     正 n 边形面积公式, 不依赖 Point/Line\n// 正 n 边形面积: A = n * l^2 / (4 * tan(pi / n)), n >= 3\nDB regular_polygon_area(ll n, DB l) {\n\treturn (DB)n * l * l / (4.0L * tanl(PI / (DB)n));\n}\n"
- trigger: :isInPolygon
  label: 点在多边形内 (奇偶射线法)
  replace: "// === isInPolygon (espanso :isInPolygon, 拆自 :Point)\n//     依赖: :Point 基础模板 (CCW + Line + rng)\n// 2 是包含，1是在线上，0是出界\ntemplate<class T>\nint isInPolygon(Point<T> p, const vector<Point<T> > &poly) {\n\tll res = 0;\n\tLine<T> l(p, p + Point<T>(2e18, rng()));\n\tfor (int i = 0; i < poly.size(); ++i) {\n\t\tint i1 = (i + 1) % SZ(poly);\n\t\tLine<T> seg = Line<T>(poly[i], poly[i1]);\n\t\tint ccw = CCW(p, seg);\n\t\tif (ccw == ON_SEGMENT) {\n\t\t\treturn 1;\n\t\t}\n\t\tif (isSegIntersect(l, seg)) {\n\t\t\t++res;\n\t\t}\n\t}\n\t// 奇偶法则，多边形内点的射线一定只会经过奇数条边。\n\tif (res & 1) return 2;\n\treturn 0;\n}\n"
- trigger: :gen_convex_polygon
  label: 凸包生成
  replace: "\n// T 为 Point 坐标类型, 整数 (ll) 或浮点 (DB) 自动分支:\n//   T 整数 : cos/sin 后 llround 钉整数, 返回 vector<Point<ll>>\n//   T 浮点 : 保留 cos/sin 原浮点值,    返回 vector<Point<DB>>\n// 坐标范围: x, y ∈ [-R, R] (椭圆 (R cos θ, R/asp sin θ), asp ∈ [1,3], 内嵌半径 R 圆盘;\n//          旋转 phi 不变半径; llround 边界最多 ±1, 整体 [-R, R] 安全).\ntemplate<class T>\nvector<Point<T> > gen_convex_polygon(int N, bool allow_collinear, ll R) {\n\t// 退化 case (N <= 2): 主循环对 N <= 2 必死循环\n\tif (N <= 2) {\n\t\tset<pair<T, T> > S;\n\t\twhile ((int) S.size() < N) {\n\t\t\tif constexpr (is_floating_point_v<T>) {\n\t\t\t\tS.insert({\n\t\t\t\t\tuniform_real_distribution<T>(-(T) R, (T) R)(rng),\n\t\t\t\t\tuniform_real_distribution<T>(-(T) R, (T) R)(rng)\n\t\t\t\t});\n\t\t\t} else {\n\t\t\t\tS.insert({\n\t\t\t\t\tuniform_int_distribution<T>(-(T) R, (T) R)(rng),\n\t\t\t\t\tuniform_int_distribution<T>(-(T) R, (T) R)(rng)\n\t\t\t\t});\n\t\t\t}\n\t\t}\n\t\tvector<Point<T> > A;\n\t\tfor (auto [x, y]: S) A.push_back(Point<T>(x, y));\n\t\treturn A;\n\t}\n\tDB phi = uniform_real_distribution<DB>(0, 2 * PI)(rng);\n\tDB asp = uniform_real_distribution<DB>(1.0, 3.0)(rng); // 1=圆, >1=椭圆\n\tDB cp = cos(phi), sp = sin(phi);\n\twhile (true) {\n\t\tvector<DB> th(N);\n\t\tfor (auto &t: th) t = uniform_real_distribution<DB>(0, 2 * PI)(rng);\n\t\tsort(th.begin(), th.end());\n\t\tvector<Point<T> > A(N);\n\t\tfor (int i = 0; i < N; ++i) {\n\t\t\tDB x = R * cos(th[i]), y = R / asp * sin(th[i]); // 椭圆\n\t\t\tDB rx = cp * x - sp * y, ry = sp * x + cp * y; // 旋转 phi\n\t\t\tif constexpr (is_floating_point_v<T>) {\n\t\t\t\tA[i] = Point<T>(rx, ry);\n\t\t\t} else {\n\t\t\t\tA[i] = Point<T>((T) llround(rx), (T) llround(ry));\n\t\t\t}\n\t\t}\n\t\tbool ok = true;\n\t\tfor (int i = 0; i < N && ok; ++i) {\n\t\t\tint j = (i + 1) % N, k = (i + 2) % N;\n\t\t\tif (A[i] == A[j]) {\n\t\t\t\tok = false;\n\t\t\t\tbreak;\n\t\t\t}\n\t\t\tauto c = cross(A[i], A[j], A[k]);\n\t\t\tif (allow_collinear ? c < 0 : c <= 0) {\n\t\t\t\tok = false;\n\t\t\t\tbreak;\n\t\t\t}\n\t\t}\n\t\tif (ok) return A;\n\t}\n}\n"
- trigger: :convexRotatingCalipers
  label: 凸包旋转卡壳工具包
  replace: "\n// 以逆时针顺序给定一个 n 个点的凸多边形（可能存在三点共线），求它的直径。\n// AC https://qoj.ac/submission/2321607\ntemplate<class T>\nlong double convex_diameter(const vector<Point<T> > &A) {\n#ifdef LOCAL\n\tvector<Point<T> > B(all(A));\n\tassert(isConvex(B,false));\n#endif\n\t// 退化特判\n\tif (SZ(A) <= 1) return 0;\n\tif (SZ(A) == 2) return sqrtL(distancePPNorm(A[0], A[1]));\n\t// A = make_convex_hull(A);\n\tauto ans = distancePPNorm(A[0], A[0]);\n\tauto area = [&](ll l, ll r) {\n\t\tauto res = cross(A[r], A[(r + 1) % SZ(A)], A[l]);\n\t\tif (res < 0) res = -res;\n\t\treturn res;\n\t};\n\tauto need_l_move = [&](ll l, ll r) -> bool {\n\t\tif (area(l, r) <= area((l + 1) % SZ(A), r)) {\n\t\t\treturn true;\n\t\t}\n\t\treturn false;\n\t};\n\t// l 是这个我们的点，r 是我们的这个边\n\tfor (int l = 0, r = 0; r < SZ(A); ++r) {\n\t\twhile (need_l_move(l, r)) {\n\t\t\tl = (l + 1) % SZ(A);\n\t\t}\n\t\tans = max(ans, distancePPNorm(A[l], A[r]));\n\t\tans = max(ans, distancePPNorm(A[l], A[(r + 1) % SZ(A)]));\n\t}\n\treturn sqrtL(ans);\n}\n\n// AC https://www.luogu.com.cn/record/276669701\ntemplate<class T>\npair<int, int> furthest_pair_of_point(vector<Point<T> > A) {\n\tA = make_convex_hull(A, true, HULL_FULL);\n\t// 退化特判\n\tif (SZ(A) == 1) return {A[0].id, A[0].id};\n\tif (SZ(A) == 2) return {A[0].id, A[1].id};\n\tdecltype(distancePPNorm(A[0], A[0])) ans = 0;\n\tauto area = [&](ll l, ll r) {\n\t\tauto res = cross(A[r], A[(r + 1) % SZ(A)], A[l]);\n\t\tif (res < 0) res = -res;\n\t\treturn res;\n\t};\n\tauto need_l_move = [&](ll l, ll r) -> bool {\n\t\tif (area(l, r) <= area((l + 1) % SZ(A), r)) {\n\t\t\treturn true;\n\t\t}\n\t\treturn false;\n\t};\n\t// l 是这个我们的点，r 是我们的这个边\n\tpair<int, int> ans_pair;\n\tfor (int l = 0, r = 0; r < SZ(A); ++r) {\n\t\twhile (need_l_move(l, r)) {\n\t\t\tl = (l + 1) % SZ(A);\n\t\t}\n\t\tif (distancePPNorm(A[l], A[r]) > ans) {\n\t\t\tans = distancePPNorm(A[l], A[r]);\n\t\t\tans_pair = {A[l].id, A[r].id};\n\t\t}\n\t\tint rn = (r + 1) % SZ(A);\n\t\tif (distancePPNorm(A[l], A[rn]) > ans) {\n\t\t\tans = distancePPNorm(A[l], A[rn]);\n\t\t\tans_pair = {A[l].id, A[rn].id};\n\t\t}\n\t}\n\treturn ans_pair;\n}\n\n// 最小矩形覆盖：返回 {面积, 4 个矩形顶点（以底边 BL 起、CCW）}\n// 前置：T = DB（reconstruct 走 getintersect 必须浮点）\n// AC https://www.luogu.com.cn/record/276877086\npair<DB, array<Point<DB>, 4> > minimum_rectangle_cover(vector<Point<DB> > A) {\n\tA = make_convex_hull(A, true, HULL_FULL);\n\tint n = SZ(A);\n\tauto e_of = [&](int i) { return A[(i + 1) % n] - A[i]; }; // 第 i 条凸包边的方向向量\n\tint up = 0, l = 0, r = 0;\n\tusing Type = decltype(dot(A[0], A[0]));\n\tType mn_area = 0;\n\tdo {\n\t\tauto e0 = e_of(0);\n\t\tfor (int i = 0; i < SZ(A); ++i) {\n\t\t\t// 同一个底，面积越大，高自然越大\n\t\t\tif (cross(e0, A[i] - A[0]) > cross(e0, A[up] - A[0])) {\n\t\t\t\tup = i;\n\t\t\t}\n\t\t\tif (dot(e0, A[i] - A[0]) > dot(e0, A[r] - A[0])) {\n\t\t\t\tr = i;\n\t\t\t}\n\t\t\tif (dot(e0, A[i] - A[0]) < dot(e0, A[l] - A[0])) {\n\t\t\t\tl = i;\n\t\t\t}\n\t\t}\n\t\tauto w = dot(e0, A[r] - A[l]);\n\t\tauto h = cross(e0, A[up] - A[0]);\n\t\tmn_area = w * h;\n\t} while (false);\n\tint bestL = l, bestR = r, bestE = 0, bestUp = up;\n\tfor (int edge = 0; edge < SZ(A); ++edge) {\n\t\tPoint<DB> e = e_of(edge);\n\t\twhile (cross(e, A[(up + 1) % SZ(A)] - A[edge]) > cross(e, A[up] - A[edge]))\n\t\t\tup = (up + 1) % SZ(A);\n\t\twhile (dot(e, A[(r + 1) % SZ(A)] - A[edge]) > dot(e, A[r] - A[edge]))\n\t\t\tr = (r + 1) % SZ(A);\n\t\twhile (dot(e, A[(l + 1) % SZ(A)] - A[edge]) < dot(e, A[l] - A[edge]))\n\t\t\tl = (l + 1) % SZ(A);\n\t\t// 这个宽和高均乘以了 e 的长度\n\t\tauto w = dot(e, A[r] - A[l]);\n\t\tauto h = cross(e, A[up] - A[edge]);\n\t\tauto area = w * h;\n\t\tif (area * e_of(bestE).norm() < mn_area * e.norm()) {\n\t\t\tmn_area = area;\n\t\t\t// mn_area_edge = e.norm();\n\t\t\ttie(bestL, bestR, bestE, bestUp) = make_tuple(l, r, edge, up);\n\t\t}\n\t}\n\t// 还原 4 顶点 = 4 条切线两两相交（4 条线方向：底/顶 ∥ e；左/右 ∥ e^⊥）\n\tPoint e = e_of(bestE);\n\tPoint e_perp(-e.y, e.x); // CCW 90° 旋转\n\tLine bottom(A[bestE], A[bestE] + e);\n\tLine top_(A[bestUp], A[bestUp] + e);\n\tLine right_(A[bestR], A[bestR] + e_perp);\n\tLine left_(A[bestL], A[bestL] + e_perp);\n\tarray<Point<DB>, 4> corners = {\n\t\tgetintersect(bottom, left_), // BL\n\t\tgetintersect(bottom, right_), // BR\n\t\tgetintersect(top_, right_), // TR\n\t\tgetintersect(top_, left_), // TL\n\t};\n\treturn {(long double) mn_area / e.norm(), corners};\n}\n"
- trigger: :halfPlaneCut
  label: 半平面交
  replace: "\n// AC https://vjudge.net/solution/69843663\ntemplate<class T>\nvector<Point<DB> > half_plane_cut(vector<Line<T> > lines) {\n\tunique_line_polar_angle_sort(lines);\n\tdeque<Line<T> > q;\n\tfor (auto l: lines) {\n\t\tif (SZ(q) < 2) {\n\t\t\tq.push_back(l);\n\t\t\tcontinue;\n\t\t}\n\t\tauto checkBack = [&]() {\n\t\t\tauto p = getintersect(q.rbegin()[0], q.rbegin()[1]);\n\t\t\tif (CCW(p, l) != CLOCKWISE) return true;\n\t\t\treturn false;\n\t\t};\n\t\twhile (SZ(q) >= 2 && !checkBack()) {\n\t\t\tq.pop_back();\n\t\t}\n\t\tauto checkFront = [&]() {\n\t\t\tauto p = getintersect(q.begin()[0], q.begin()[1]);\n\t\t\tif (CCW(p, l) != CLOCKWISE) return true;\n\t\t\treturn false;\n\t\t};\n\t\twhile (SZ(q) >= 2 && !checkFront()) {\n\t\t\tq.pop_front();\n\t\t}\n\t\tq.push_back(l);\n\t}\n\t// wrap-around: 用队首线再清一遍队尾\n\twhile (SZ(q) >= 3) {\n\t\tauto p = getintersect(q.rbegin()[0], q.rbegin()[1]);\n\t\tif (CCW(p, q.front()) != CLOCKWISE) break;\n\t\tq.pop_back();\n\t}\n\t// 对称地: 用队尾线再清一遍队首 (有的写法会加, 多数情况下前面 checkFront 已经清干净, 但保险起见)\n\twhile (SZ(q) >= 3) {\n\t\tauto p = getintersect(q.begin()[0], q.begin()[1]);\n\t\tif (CCW(p, q.back()) != CLOCKWISE) break;\n\t\tq.pop_front();\n\t}\n\tif (SZ(q) < 3) return {};\n\tvector<Point<DB> > poly;\n\tfor (int i = 0; i < SZ(q); ++i) {\n\t\tauto p = getintersect(q[i], q[(i + 1) % SZ(q)]);\n\t\tpoly.push_back(p);\n\t}\n\treturn poly;\n}\n"
- trigger: :ccwAngle
  label: ccw_angle 逆时针弧角 (标量 + 向量两版)
  replace: "// === ccw_angle (espanso :ccwAngle, 拆自 :Circle)\n//     依赖: :Point 基础模板 (向量版用到 cross / dot)\n// 圆弧 from -> to 的逆时针弧角差 [0, 2pi), 不是 abs(to - from)\nstatic DB ccw_angle(DB theta_from, DB theta_to) {\n\tDB d = std::fmod(theta_to - theta_from, 2 * PI);\n\tif (d < 0) d += 2 * PI;\n\treturn d;\n}\n\n// 向量版: 从方向向量 from 逆时针转到方向向量 to 的角 [0, 2pi).\n// 用单次 atan2(cross, dot) (cross 当 sin 分量 / dot 当 cos 分量), 比两次 atan2 相减稳.\ntemplate<class T, class U>\nstatic DB ccw_angle(Point<T> from, Point<U> to) {\n\tDB d = std::atan2((DB)cross(from, to), (DB)dot(from, to));\n\tif (d < 0) d += 2 * PI;\n\treturn d;\n}\n"
- trigger: :Circle
  label: 圆类
  replace: "// ===== 圆 (Circle) =====\n\nenum CircleRelation {\n\t// 表示这个公切线个数; COINCIDENT (-1) 是重合 sentinel (无穷多公切线)\n\tCOINCIDENT = -1,\n\tONE_CONTAINS_OTHER = 0,\n\tINTERNALLY_TANGENT = 1,\n\tINTERSECTING = 2,\n\tEXTERNALLY_TANGENT = 3,\n\tEXTERNALLY_APART = 4,\n};\n\ntemplate<class T>\nstruct Circle {\n\tPoint<T> c;\n\tT r;\n\n\tCircle() = default;\n\n\tCircle(Point<T> c_, T r_) : c(c_), r(r_) {\n\t}\n\n\t// Circle<U> -> Circle<T> 隐式 (common_type 单调升档方向, 同 Point converting ctor)\n\ttemplate<class U, class = enable_if_t<is_same_v<common_type_t<U, T>, T> > >\n\tCircle(const Circle<U> &o) : c(o.c), r(o.r) {\n\t}\n\n\t// 圆上极角 theta 处的点: c + (r cos theta, r sin theta)\n\t// theta 不限定范围 (允许负值 / > 2pi), 始终走 cos/sin 不抛 assert\n\tPoint<DB> point_at(DB theta) const {\n\t\tDB s, co;\n\t\t__builtin_sincos(theta, &s, &co); // 一次算俩, 比分开 sin+cos 再快一倍\n\t\treturn Point<DB>((DB)c.x + (DB)r * co,\n\t\t                 (DB)c.y + (DB)r * s);\n\t\t// return Point<DB>((DB)c.x + (DB)r * cosl(theta),\n\t\t// (DB)c.y + (DB)r * sinl(theta));\n\t}\n\n\tvoid check_theta(DB theta) const {\n#ifdef LOCAL\n\t\tassert(theta >= -eps && theta <= 2 * PI + eps);\n#endif\n\t\t(void)theta;\n\t}\n\n\t// point_at 的逆: 点 p 相对圆心的极角, 归一化到 [0, 2pi)\n\t// 满足 point_at(theta_at(p)) == p (p 在圆上时)\n\ttemplate<class U>\n\tDB theta_at(const Point<U> &p) const {\n\t\tcheck_on_circle(p);\n\t\tDB t = atan2l((DB)p.y - (DB)c.y, (DB)p.x - (DB)c.x);\n\t\tif (t < 0) t += 2 * PI;\n\t\treturn t;\n\t}\n\n\ttemplate<class U>\n\tvoid check_on_circle(const Point<U> &p) const {\n#ifdef LOCAL\n\t\tassert(sgn(distancePPNorm(p, c) - squared(r)) == 0);\n#endif\n\t\t(void)p;\n\t}\n\n\tDB arc_length(DB theta) const {\n\t\tcheck_theta(theta);\n\t\treturn (DB)r * theta;\n\t}\n\n\tDB chord_length(DB theta) const {\n\t\tcheck_theta(theta);\n\t\treturn 2.0L * (DB)r * sinl(theta / 2);\n\t}\n\n\t// 弓形高 (sagitta) = r (1 - cos(theta / 2))\n\tDB sagitta(DB theta) const {\n\t\tcheck_theta(theta);\n\t\treturn (DB)r * (1.0L - cosl(theta / 2));\n\t}\n\n\t// 扇形面积 = (1/2) r^2 theta\n\tDB sector_area(DB theta) const {\n\t\tcheck_theta(theta);\n\t\treturn 0.5L * (DB)r * r * theta;\n\t}\n\n\t// 弓形面积 = (1/2) r^2 (theta - sin theta)\n\t// AC https://vjudge.net/solution/69814399 (Area of the Devil)\n\tDB segment_area(DB theta) const {\n\t\tcheck_theta(theta);\n\t\treturn 0.5L * (DB)r * r * (theta - sinl(theta));\n\t}\n\n\t// AC https://vjudge.net/solution/69993234 使用 sgn 避免精度问题\n\ttemplate<class U>\n\tbool is_point_in_circle(const Point<U> &p) {\n\t\treturn sgn(distancePPNorm(p, c) - squared(r)) <= 0;\n\t}\n\n\tfriend istream &operator>>(istream &is, Circle &c) {\n\t\tis >> c.c >> c.r;\n\t\treturn is;\n\t}\n\n\t// 用于 debug\n\tfriend ostream &operator<<(ostream &os, Circle &c) {\n\t\tos << \"<\" << c.c << \",\" << c.r << \">\";\n\t\treturn os;\n\t}\n};\n\n// 两点为直径的圆: 圆心为两点中点, 半径为两点距离的一半\n// 退化 (两点重合): 半径为 0, 圆心即该点 (退化为点, 是合法结果, 非 sentinel)\nCircle<DB> circle_from_diameter(Point<DB> a, Point<DB> b) {\n\treturn Circle<DB>((a + b) / 2, distancePP(a, b) / 2);\n}\n"
- trigger: :circleRelation
  label: circle_relation (两圆关系判定 squared 整型)
  replace: "// === circle_relation (espanso :circleRelation, 拆自 :Circle)\n//     依赖: :Circle (CircleRelation enum + Circle 类)\n// AC https://vjudge.net/solution/69918813 (CGL_7_A 公切线数)\ntemplate<class T>\nCircleRelation circle_relation(const Circle<T> &a, const Circle<T> &b) {\n\tauto d2 = distancePPNorm(a.c, b.c);\n\tauto sum = squared(a.r + b.r);\n\tauto dif = squared(a.r - b.r);\n\t// 重合 (同心 + 同径): 无穷多公切线, 单独 sentinel\n\tif (sgn(d2) == 0 && sgn(a.r - b.r) == 0) return COINCIDENT;\n\tif (sgn(d2 - sum) > 0) return EXTERNALLY_APART;\n\tif (sgn(d2 - sum) == 0) return EXTERNALLY_TANGENT;\n\tif (sgn(d2 - dif) > 0) return INTERSECTING;\n\tif (sgn(d2 - dif) == 0) return INTERNALLY_TANGENT;\n\treturn ONE_CONTAINS_OTHER;\n}\n"
- trigger: :incircleOutTriangleCircle
  label: 三角形内接圆 + 外接圆
  replace: "// === incircle_triangle + outcircle_triangle (espanso :incircleOutcircle, 拆自 :Circle)\n//     依赖: :Point + :Circle\n// 三角形内接圆: 圆心为加权重心 (权 = 对边长), 半径 = min(圆心到三边距离)\n// 退化 (共线 / 任意两点重合, cross == 0): 内接圆不存在, 返回 r=0 sentinel (圆心位置无意义)\n// AC https://vjudge.net/solution/69920478 (CGL_7_B)\nCircle<DB> incircle_triangle(Point<DB> a, Point<DB> b, Point<DB> c) {\n\tif (sgn(cross(a, b, c)) == 0) return Circle<DB>(a, 0);\n\tDB ab = distancePP(a, b), ac = distancePP(a, c), bc = distancePP(b, c);\n\tPoint<DB> I = (a * bc + ac * b + ab * c) / (ab + ac + bc);\n\treturn Circle<DB>(I, min({distancePS(I, Line(a, c)), distancePS(I, Line(a, b)), distancePS(I, Line(b, c))}));\n}\n\n// 三角形外接圆: 圆心是两条边的垂直平分线交点\n// 退化 (共线 / 任意两点重合, cross == 0): 外接圆不存在, 改返回覆盖三点的最小圆 (以最远点对为直径)\n// AC https://vjudge.net/solution/69927652 (CGL_7_C)\nCircle<DB> outcircle_triangle(Point<DB> a, Point<DB> b, Point<DB> c) {\n\tif (sgn(cross(a, b, c)) == 0) {\n\t\tDB ab = distancePP(a, b), ac = distancePP(a, c), bc = distancePP(b, c);\n\t\tif (ab >= ac && ab >= bc) return circle_from_diameter(a, b);\n\t\tif (ac >= bc) return circle_from_diameter(a, c);\n\t\treturn circle_from_diameter(b, c);\n\t}\n\tPoint<DB> abMid = (a + b) / 2, acMid = (a + c) / 2;\n\tPoint<DB> vecAb = (b - a).rot90(), vecAc = (c - a).rot90();\n\tPoint<DB> I = getintersect(Line(abMid, abMid + vecAb), Line(acMid, acMid + vecAc));\n\treturn Circle<DB>(I, distancePP(I, a));\n}\n"
- trigger: :getCrossPointsCL
  label: 圆与直线交点 + 关系判定 (重点 getCrossPointsCL)
  replace: "// === circle_line_relation + getCrossPointsCL (espanso :getCrossPointsCL, 拆自 :Circle)\n//     依赖: :Point + :Circle\n// 直线与圆的交点个数: 0 相离 / 1 相切 / 2 相交.\n// d = |cross(p2 - p1, c - p1)| / |p2 - p1|, 两边平方 + 乘 |p2-p1|^2 走整数:\n//   cross^2 vs r^2 * |p2 - p1|^2\n// AC https://vjudge.net/solution/69928470 (CGL_7_D)\n// AC https://vjudge.net/solution/69929454 (EOlymp-359 弦长 = 2 * sqrt(r^2 - d^2))\ntemplate<class T>\nint circle_line_relation(const Circle<T> &cir, const Line<T> &l) {\n\t// 退化直线 (p1 == p2): 不构成有效直线, 视为无交点\n\tif (l.p1 == l.p2) return 0;\n\tauto cr2 = squared(cross(l.p1, l.p2, cir.c));\n\tauto lenSq = (l.p2 - l.p1).norm();\n\tauto rhs = squared(cir.r) * lenSq;\n\tint s = sgn(cr2 - rhs);\n\tif (s > 0) return 0;\n\tif (s == 0) return 1;\n\treturn 2;\n}\n\n// 圆与直线的交点: 投影 + 半弦长\n// 退化直线 (p1 == p2): circle_line_relation 已返 0, 这里直接吃到空集\n// AC https://vjudge.net/solution/69928470 (CGL_7_D)\ntemplate<class T>\nvector<Point<DB> > getCrossPointsCL(const Circle<T> &cir, const Line<T> &l) {\n\tint k = circle_line_relation(cir, l);\n\tif (k == 0) return {};\n\t// 得到垂足\n\tPoint<DB> F = project(cir.c, l);\n\tif (k == 1) return {F};\n\tPoint<DB> d = l.p2 - l.p1;\n\tPoint<DB> dhat = d / d.abs();\n\t// 斜边 R 与直角边（圆心和垂足）得到另外一直角边（交点与垂足）\n\tDB h = sqrtl(max((DB)0, (DB)cir.r * cir.r - (cir.c - F).norm()));\n\treturn {F - dhat * h, F + dhat * h};\n}\n"
- trigger: :getCrossPointsCC
  label: 两圆交点 + 关系判定 (重点 getCrossPointsCC)
  replace: "// === circle_circle_relation + getCrossPointsCC (espanso :getCrossPointsCC, 拆自 :Circle)\n//     依赖: :Point + :Circle\n// 两圆交点个数: 0 相离 / 包含 / 1 相切 (内/外) / 2 相交.\n// d^2, (r1+r2)^2, (r1-r2)^2 全走整数 squared 比较, 不开方避免精度.\n// AC https://vjudge.net/solution/69929473 (CGL_7_E)\ntemplate<class T>\nint circle_circle_relation(const Circle<T> &a, const Circle<T> &b) {\n\tauto d2 = distancePPNorm(a.c, b.c);\n\tauto sum = squared(a.r + b.r);\n\tauto dif = squared(a.r - b.r);\n\t// 重合 (同心 + 同径): 交点无穷多, 返 -1 sentinel\n\tif (sgn(d2) == 0 && sgn(a.r - b.r) == 0) return -1;\n\tif (sgn(d2 - sum) > 0) return 0;\n\tif (sgn(d2 - dif) < 0) return 0;\n\tif (sgn(d2 - sum) == 0 || sgn(d2 - dif) == 0) return 1;\n\treturn 2;\n}\n\n// 两圆交点: 连心线中点 + 半弦长沿连心线 perp 方向 +-\n// 关键 promotion 坑: `(c2.c - c1.c) / D` 若 cN 是 Point<ll>, D 是 DB, 会\n// 走 Point<ll>::operator/(Point<ll>, ll) 把 D 截成整数 -> 必须先 Point<DB> d = c2.c - c1.c.\n// AC https://vjudge.net/solution/69929473 (CGL_7_E)\n// AC https://vjudge.net/solution/69929406 (EOlymp-4779, +重合 sentinel \"3\" 由 Solve 处理)\ntemplate<class T>\nvector<Point<DB> > getCrossPointsCC(const Circle<T> &c1, const Circle<T> &c2) {\n\tint k = circle_circle_relation(c1, c2);\n\t// k = -1 重合 (无穷多) / k = 0 不交: 模板统一返回空, 重合语义由 caller 自己处理\n\tif (k <= 0) return {};\n\t// 弦中点\n\tPoint<DB> d = c2.c - c1.c;\n\tDB D = d.abs();\n\tPoint<DB> dhat = d / D;\n\tDB a = ((DB)c1.r * c1.r - (DB)c2.r * c2.r + D * D) / (2 * D);\n\tPoint<DB> M = (Point<DB>)c1.c + dhat * a;\n\tif (k == 1) return {M};\n\t// 沿连心线垂直方向 +- 半弦长\n\tDB h = sqrtl(max((DB)0, (DB)c1.r * c1.r - a * a));\n\tPoint<DB> nhat = dhat.rot90();\n\treturn {M + nhat * h, M - nhat * h};\n}\n"
- trigger: :circleTangent
  label: 点对圆切点 + 两圆公切线
  replace: "// === getTangentPoints + getCommonTangentPoints (espanso :circleTangent, 拆自 :Circle)\n//     依赖: :Point + :Circle\n// 过一点圆的切点 = 原圆 cap 辅助圆 (圆心 P, 半径 L = sqrt(|PC|^2 - r^2))\n// 几何依据: 切线性质 PT perp CT, 三角形 PCT 是直角三角形 (直角在 T)\n//   |PT| = L, |CT| = r, |PC| 是斜边\n// 切点 T 同时落在两个圆上: 原圆 (C, r) 和辅圆 (P, L)\n// 退化: P 在圆内 (含圆心) 返回空; P 在圆上唯一切点就是 P 自身\n// AC https://vjudge.net/solution/69929512 (CGL_7_F)\nvector<Point<DB> > getTangentPoints(Point<ll> P, Circle<ll> cir) {\n\tauto d2 = (P - cir.c).norm();\n\tauto r2 = (__int128_t)cir.r * cir.r;\n\tif (d2 < r2) return {};\n\tif (d2 == r2) return { Point<DB>((DB)P.x, (DB)P.y) };\n\tDB Lsq = (DB)d2 - (DB)r2;\n\tDB L = sqrtl(max((DB)0, Lsq));\n\tCircle<DB> orig((Point<DB>)cir.c, (DB)cir.r);\n\tCircle<DB> aux((Point<DB>)P, L);\n\treturn getCrossPointsCC(orig, aux);\n}\n\n// 两圆公切线在 c1 上的切点\n// 推导: 切点 T1 = c1.c + r1 * n, 其中 n 是 (T1-c1.c) 方向的单位法向量.\n//       外公切: 两边法向同向, T2 = c2.c + r2 * n, 且 (T2-T1) perp n.\n//         (c2.c - c1.c) . n = r1 - r2  =>  cos_alpha = (r1 - r2) / D, sin_alpha = +-sqrt(1-cos^2).\n//       内公切: 两边法向反向, T2 = c2.c - r2 * n.\n//         (c2.c - c1.c) . n = r1 + r2  =>  cos_alpha = (r1 + r2) / D.\n//       n = cos_alpha * u + sin_alpha * u_perp, u = (c2.c - c1.c) / D.\n// 退化:\n//   |cos| > 1 该类无切 (内含 / 包含); sin = 0 该类只 1 条 (内/外切 临界).\n//   D = 0 (同心): 不同径无公切; 重合时实际无穷多, 模板统一返空.\n// AC https://vjudge.net/solution/69929554 (CGL_7_G)\nvector<Point<DB> > getCommonTangentPoints(Circle<ll> c1, Circle<ll> c2) {\n\tvector<Point<DB> > ret;\n\tPoint<DB> d = c2.c - c1.c;\n\tDB D = d.abs();\n\tif (D < eps) return ret;\n\tPoint<DB> u = d / D;\n\tPoint<DB> u_perp = u.rot90();\n\tDB r1 = c1.r, r2 = c2.r;\n\tauto add = [&](DB cos_a) {\n\t\tif (cos_a > 1 + eps || cos_a < -1 - eps) return;\n\t\tDB s = sqrtl(max((DB)0, 1 - cos_a * cos_a));\n\t\tPoint<DB> c1d = c1.c;\n\t\tif (s < eps) {\n\t\t\tPoint<DB> n = u * cos_a;\n\t\t\tret.push_back(c1d + n * r1);\n\t\t} else {\n\t\t\tret.push_back(c1d + (u * cos_a + u_perp * s) * r1);\n\t\t\tret.push_back(c1d + (u * cos_a - u_perp * s) * r1);\n\t\t}\n\t};\n\tadd((r1 - r2) / D); // 外公切\n\tadd((r1 + r2) / D); // 内公切\n\tsort(ret.begin(), ret.end());\n\t// 内/外切临界 + r=0 退化时 4 个候选可能近重合, unique 去重 (走 Point<DB> ==, eps tolerant)\n\tret.erase(unique(ret.begin(), ret.end()), ret.end());\n\treturn ret;\n}\n"
- trigger: :twoCircleArea
  label: 两圆相交面积
  replace: "// === two_circle_intersect_area (espanso :twoCircleArea, 拆自 :Circle)\n//     依赖: :Point + :Circle (Circle 成员 sector_area / segment_area)\n// 两圆交集面积: 两个弓形 segment_area 之和.\n// 推导: 设公共弦的中点 M, 公共弦把 c1 切成两个弓形 (cap), 取靠 c2 的那个;\n//   c2 同理. 二者相加 = 交集面积.\n//   c1 的相关圆心角 theta1 = 2 * acos((D^2 + r1^2 - r2^2) / (2 * D * r1))\n//   c2 的相关圆心角 theta2 = 2 * acos((D^2 + r2^2 - r1^2) / (2 * D * r2))\n//   segment_area = 0.5 * r^2 * (theta - sin theta).\n// 边界:\n//   D >= r1 + r2 : 不交 -> 0\n//   D <= |r1 - r2|: 一圆完全包含另一圆 -> pi * r_min^2\n// AC https://vjudge.net/solution/69929585 (CGL_7_I)\ntemplate<class T>\nDB two_circle_intersect_area(Circle<T> c1, Circle<T> c2) {\n\tDB D = distancePP(c1.c, c2.c);\n\tDB r1 = c1.r, r2 = c2.r;\n\tif (sgn(D - r1 - r2) >= 0) return 0;\n\tDB diff = abs(r1 - r2);\n\tif (sgn(D - diff) <= 0) {\n\t\tDB rmin = min(r1, r2);\n\t\treturn PI * rmin * rmin;\n\t}\n\tDB t1 = 2 * acosl((D * D + r1 * r1 - r2 * r2) / (2 * D * r1));\n\tDB t2 = 2 * acosl((D * D + r2 * r2 - r1 * r1) / (2 * D * r2));\n\treturn 0.5L * r1 * r1 * (t1 - sinl(t1)) + 0.5L * r2 * r2 * (t2 - sinl(t2));\n}\n"
- trigger: :polygonCircleArea
  label: 多边形与圆相交面积
  replace: "// === tri_disk_signed_area + polygon_circle_intersect_area (espanso :polygonCircleArea, 拆自 :Circle)\n//     依赖: :Point + :Circle (Circle 成员) + :circleLineRelation (getCrossPointsCL)\n// 单条有向三角形 (O, A, B) 与 disk(O, r) 的有向面积.\n// 默认 A, B 已平移到以圆心 O 为原点的坐标系.\n// 把三角形按 AB 与圆周的交点切片: 内 -> 真三角形, 外 -> 扇形面积.\n// (是 polygon_circle_intersect_area 的内部 helper, 同一个 AC.)\n// AC https://vjudge.net/solution/69929628 (CGL_7_H)\nDB tri_disk_signed_area(Point<DB> A, Point<DB> B, DB r) {\n\tif (A == B) return 0; // 零长度边, 三角形退化, 有向面积为 0\n\tDB rA = A.abs(), rB = B.abs();\n\tbool aIn = sgn(rA - r) <= 0;\n\tbool bIn = sgn(rB - r) <= 0;\n\tauto ang = [](Point<DB> P, Point<DB> Q) {\n\t\treturn atan2l((DB)cross(P, Q), (DB)dot(P, Q));\n\t};\n\tif (aIn && bIn) {\n\t\t// 整段都在圆内, 真三角形面积\n\t\treturn 0.5L * (DB)cross(A, B);\n\t}\n\t// 求 A + t*(B-A) 落圆上的 t: |A + t d|^2 = r^2\n\tPoint<DB> d = B - A;\n\tDB dd = (DB)dot(d, d);\n\tDB dA = (DB)dot(d, A);\n\tDB AA = (DB)dot(A, A);\n\tDB disc = dA * dA - dd * (AA - r * r);\n\tdisc = max(disc, (DB)0);\n\tDB sq = sqrtl(disc);\n\tDB t1 = (-dA - sq) / dd, t2 = (-dA + sq) / dd;\n\tif (!aIn && !bIn) {\n\t\t// 两端都在外, 看弦区间 [t1, t2] 与段 [0, 1] 是否相交\n\t\tif (sgn(t2) < 0 || sgn(t1 - 1) > 0 || sgn(disc) <= 0) {\n\t\t\t// 不穿圆, 整段对应一个扇形 (从 OA 转到 OB)\n\t\t\treturn 0.5L * r * r * ang(A, B);\n\t\t}\n\t\tt1 = max(t1, (DB)0);\n\t\tt2 = min(t2, (DB)1);\n\t\tPoint<DB> P1 = A + d * t1;\n\t\tPoint<DB> P2 = A + d * t2;\n\t\treturn 0.5L * r * r * ang(A, P1) + 0.5L * (DB)cross(P1, P2)\n\t\t     + 0.5L * r * r * ang(P2, B);\n\t}\n\tif (aIn && !bIn) {\n\t\t// A 内 B 外, 段 [0,1] 在 [t1, t2] 内的部分: t >= 0 (因为 A 内 t1 < 0), 取 t2\n\t\tPoint<DB> P = A + d * t2;\n\t\treturn 0.5L * (DB)cross(A, P) + 0.5L * r * r * ang(P, B);\n\t}\n\t// !aIn && bIn: A 外 B 内, t1 是入口\n\tPoint<DB> P = A + d * t1;\n\treturn 0.5L * r * r * ang(A, P) + 0.5L * (DB)cross(P, B);\n}\n\n// 简单多边形 (CCW) 与圆的交集面积. 累加每条边的 tri_disk_signed_area.\n// AC https://vjudge.net/solution/69929628 (CGL_7_H)\nDB polygon_circle_intersect_area(Circle<DB> cir, const vector<Point<ll> > &poly) {\n\tDB res = 0;\n\tint n = poly.size();\n\tfor (int i = 0; i < n; ++i) {\n\t\tPoint<DB> A = poly[i] - cir.c;\n\t\tPoint<DB> B = poly[(i + 1) % n] - cir.c;\n\t\tres += tri_disk_signed_area(A, B, cir.r);\n\t}\n\treturn abs(res);\n}\n"
- trigger: :minCircleCover
  label: 最小圆覆盖 (随机增量法)
  replace: "// === min_circle_cover (espanso :minCircleCover, 配 :Circle 的随机增量最小圆覆盖)\n//     依赖: :Point + :Circle + :incircleOutTriangleCircle\n// AC https://vjudge.net/solution/69974356 最小圆覆盖\ntemplate<class T>\nCircle<DB> min_circle_cover(vector<Point<T> > A) {\n\t// 先打乱\n\tshuffle(all(A), rng);\n\tCircle<DB> res(A[0], 0);\n\tfor (int i = 1; i < SZ(A); ++i) {\n\t\tif (!res.is_point_in_circle(A[i])) {\n\t\t\tres = Circle<DB>(A[i], 0);\n\t\t\t// 注意，我们的这个后缀点之前已经考虑过了，实际上我们是要覆盖前缀点\n\t\t\tfor (int j = 0; j < i; ++j) {\n\t\t\t\tif (!res.is_point_in_circle(A[j])) {\n\t\t\t\t\tres = circle_from_diameter(A[i], A[j]);\n\t\t\t\t\tfor (int k = 0; k < j; ++k) {\n\t\t\t\t\t\tif (!res.is_point_in_circle(A[k])) {\n\t\t\t\t\t\t\tres = outcircle_triangle(A[i], A[j], A[k]);\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\treturn res;\n}"
